{
  "name": "browser-switcher",
  "version": "0.1.6",
  "private": true,
  "type": "module",
  "scripts": {
    "build:about": "node scripts/build-about.mjs",
    "tauri": "tauri",
    "dev": "npm run build:about && tauri dev",
    "dev:about": "npm run build:about && node scripts/dev-about.mjs",
    "build": "npm run build:about && tauri build",
    "test:about": "node tests/about-html.test.mjs",
    "test:rust": "npm run build:about && cargo test --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
